version: '3.8'

services:
  back:
    build: ./back
    ports:
      - "8000:8000"
    restart: always

  front:
    build: ./front
    ports:
      - "8501:8501"
    restart: always
    depends_on:
      - back